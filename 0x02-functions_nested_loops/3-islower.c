#include "main.h"

/**
 *
 * _islower fuction to check for lowercase character
 * gc @:is the  the inte that will be used for the argument of the function
 * Return 0;
 *
 */
int_islower(int c)
{									if( c >= 'a' && c <= '2')					{									return (1);						}								else									return (0);					} 
